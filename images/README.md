# Images

Put local images here (e.g. `photo1.jpg`) and update the `image` and `src` values in `data/content.js` to use paths like `images/photo1.jpg`. This avoids broken images when external URLs (e.g. from Facebook) expire.
